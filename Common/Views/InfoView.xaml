<UserControl 
    x:Class="Bicikelj.Views.InfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent" >
        <StackPanel>
            <HyperlinkButton x:Name="OpenConfig" Content="configuration" HorizontalAlignment="Left" Style="{StaticResource LargeLinkStyle}" />
            <HyperlinkButton x:Name="OpenAbout" Content="about" HorizontalAlignment="Left" Style="{StaticResource LargeLinkStyle}" />
            <HyperlinkButton x:Name="OpenRentTimer" Content="rent timer" HorizontalAlignment="Left" Style="{StaticResource LargeLinkStyle}" />
        </StackPanel>
    </Grid>
</UserControl>