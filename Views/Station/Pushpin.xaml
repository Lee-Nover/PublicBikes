<UserControl 
    x:Class="Bicikelj.Views.Station.Pushpin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    xmlns:cvt="clr-namespace:Bicikelj.Converters" 
    xmlns:b="clr-namespace:Bicikelj" 
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480">

    <Grid x:Name="ContentGrid" Margin="12,0,12,12" >
        <Grid.ColumnDefinitions>
            <!--ColumnDefinition Width="Auto" /-->
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="1" x:Name="StationName" Style="{StaticResource PhoneTextNormalStyle}" />
        <TextBlock Grid.Column="1" Grid.Row="1" x:Name="Address" Style="{StaticResource PhoneTextSubtleStyle}" />
        <HyperlinkButton Grid.Column="1" Grid.Row="1" x:Name="RefreshAvailability" Content="check availability" FontSize="{StaticResource PhoneFontSizeSmall}" />
    </Grid>

</UserControl>